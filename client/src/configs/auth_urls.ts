const API_URL = 'http://localhost:5000/api/auth'
const IS_VERIFY_URL = `${API_URL}/is-verify`
const LOGIN_URL = `${API_URL}/login`
const REGISTER_URL = `${API_URL}/register`

const AuthURL = {
    API_URL,
    IS_VERIFY_URL,
    LOGIN_URL,
    REGISTER_URL,
}

export default AuthURL